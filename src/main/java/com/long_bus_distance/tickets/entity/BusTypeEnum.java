package com.long_bus_distance.tickets.entity;

public enum BusTypeEnum {
    STANDARD, // Xe buýt ghế tiêu chuẩn
    SLEEPER, // Xe buýt giường nằm có tầng trên và tầng dưới (17 ghế mỗi tầng)
    LIMOUSINE // Limousine bus with upper and lower decks (17 seats per deck)
}
